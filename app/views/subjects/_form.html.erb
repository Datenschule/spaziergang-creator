  <%= f.label :name, "Name" %>
  <%= f.text_field :name %>

  <%= f.label :description, "Description" %>
  <%= f.text_area :description %>

  <%= f.label :station_id, "Station" %>
  <%= f.collection_select :station_id, current_user.stations, :id, :name, {prompt: "Optional station"} %>

  <%= f.submit %>
