<div class="container">
  <div class="columns">
    <h1 class="column col-12 mb-2"><%= @walk.name%> <%= t('walk.edit_verb') %> </h1>
  </div>
  <div class="columns">
    <div class="column col-8">
      <%= form_for :walk, method: :patch, url: walk_path(@walk) do |f| %>
        <%=  render 'walks/form',  f: f %>
      <% end %>
    </div>
  </div>
</div>
