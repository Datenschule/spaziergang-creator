<% if pages.first %>
  <ol class="walk-list">
    <% pages.sort_by(&:priority).each do |page| %>
      <%= render("pages/list_item", page: page) %>
    <% end %>
  </ol>
<% else %>
  <%=  render 'pages/empty' %>
<% end %>
